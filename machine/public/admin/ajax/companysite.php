<?php/** * 自社サイトをAJAX処理 *  * @access  public * @author  川端洋平 * @version 0.2.0 * @since   2014/11/10 *///// 設定ファイル読み込み ////require_once '../../../lib-machine.php';try {    //// 認証 ////    Auth::isAuth('member');    if ($_SERVER["REQUEST_METHOD"] == "POST") {        $data   = Req::post();        $target = Req::post('_target');        $action = Req::post('_action');        unset($data['_action'], $data['_target']);                $companysiteTable = new Companysites();        if ($action == "set") {            $companysiteTable->memberSetByCompanyId($_user['company_id'], $data);        } else { throw new Exception('処理がありません'); }        echo 'success';    }} catch (Exception $e) {    echo $e->getMessage();}